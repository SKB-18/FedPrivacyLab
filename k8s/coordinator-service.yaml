apiVersion: v1
kind: Service
metadata:
  name: fedprivacylab-coordinator
  namespace: fedprivacylab
  labels:
    app: coordinator
    component: api
spec:
  type: ClusterIP
  selector:
    app: coordinator
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
