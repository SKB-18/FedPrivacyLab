apiVersion: v1
kind: Service
metadata:
  name: fedprivacylab-dashboard
  namespace: fedprivacylab
  labels:
    app: dashboard
    component: ui
spec:
  type: LoadBalancer
  selector:
    app: dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8501
      protocol: TCP
